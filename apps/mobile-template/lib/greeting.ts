// Placeholder pure function proving the Vitest harness works end to end for
// this app -- mirrors the original web platform's Phase 0 convention of one
// placeholder test per new package before any real logic exists.
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
