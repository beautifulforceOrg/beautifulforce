// Phase 0 placeholder -- the per-client app.config.ts helpers (bundle ID,
// app name, icon/splash, EAS project ID, theme) land alongside Phase 7's
// apps/beautifulmess-mobile fork.
export const PACKAGE_NAME = "@storeforge/mobile-config";
